@model IEnumerable<AdunbiKiddies.Models.BusinessRegistration>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Merchant.CompanyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RegistrationNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LegalName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Merchant.CompanyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegistrationNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LegalName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BusinessRegistrationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BusinessRegistrationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BusinessRegistrationId })
        </td>
    </tr>
}

</table>
